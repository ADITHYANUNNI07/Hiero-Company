enum ScreenPopCount { one, two, three }

enum SlideDirection { slideUp, slideDown, slideRight, slideLeft }

enum Gender { male, female, other, none }

enum AuthType { login, register }

enum MessageType { text, audio, sticker, sharedPost, storyReply }

enum Themes { lightTheme, darkTheme }

enum Positions { left, right, top, bottom }

enum NotificationType { follow, like, app }

enum MessageTab { chats, clubs, request }
